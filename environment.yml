name: cavitymd_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - mdtraj>=1.9.7
  - mdanalysis>=2.0.0        # CRÍTICO - cargar .dcd
  - networkx>=2.6            # CRÍTICO - network analysis
  - scikit-learn>=1.0        # Para PCA
  - numpy>=1.21
  - scipy>=1.7
  - matplotlib>=3.4
  - seaborn>=0.11
  - pyyaml>=5.4              # YAML configs
  - pandas>=1.3
  - tqdm>=4.60               # 🆕 CRÍTICO - progress bars (faltaba!)
  - pytest>=6.2
  - pip
  - pip:
    - -e .